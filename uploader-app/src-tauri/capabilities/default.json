{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set granted to the main uploader window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
